# 回序项目执行约束

## 避免任务卡住

- 不在同步命令中启动长期驻留的开发服务器。
- 构建、静态检查和测试必须分开执行，并设置明确超时。
- 无输出的检查超过 30 秒应终止，改为只检查本次修改文件或使用更窄的验证方式。
- 不同时并行运行会争用 `dist`、缓存或依赖目录的命令。
- 本地服务必须使用独立端口，并在交付链接前验证页面内容及资源响应，不能只验证端口返回 200。
- 遇到超时先停止当前进程、说明阻塞点，再继续其他可验证工作，不反复等待同一命令。
